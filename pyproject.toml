[project]
name = "sw-issue"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "apache-skywalking[http]>=1.1.0",
    "django>=5.1.6",
]
